<!-- values/themes.xml -->
<resources>
    <!-- 라이트 모드 테마 설정 -->
    <style name="Theme.Socialdiary.Light" parent="Theme.Socialdiary">
        <item name="android:colorBackground">@color/appColorBackgroundLight</item>
        <item name="android:textColor">@color/appColorTextPrimaryLight</item>
    </style>

    <!-- 다크 모드 테마 설정 -->
    <style name="Theme.Socialdiary.Dark" parent="Theme.Socialdiary">
        <item name="android:colorBackground">@color/appColorBackgroundDark</item> <!-- 검정 배경 -->
        <item name="android:textColor">@color/appColorTextPrimaryDark</item> <!-- 흰색 텍스트 -->
    </style>
</resources>
